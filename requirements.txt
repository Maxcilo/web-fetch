# Web Fetch Requirements

## Python Version
Python >= 3.8

## Core Dependencies
scrapling>=0.2.0
html2text>=2020.1.16
curl-cffi>=0.5.0
browserforge>=1.0.0

## PDF Generation Dependencies (v1.3.0)
weasyprint>=68.0
markdown>=3.0
Pillow>=10.0

## System Dependencies
# curl - 用于 Jina Reader
# 通常系统自带，如果没有：
# Ubuntu/Debian: apt-get install curl
# CentOS/RHEL: yum install curl
# macOS: brew install curl

## Installation
# 基本安装
pip install scrapling html2text curl-cffi browserforge

# PDF 功能安装（v1.3.0）
pip install weasyprint markdown Pillow

## Optional Dependencies (for future enhancements)
# requests>=2.28.0  # 替代 curl，提升性能
# aiohttp>=3.8.0    # 异步支持
# redis>=4.0.0      # 缓存支持

## Development Dependencies
# pytest>=7.0.0     # 测试框架
# black>=22.0.0     # 代码格式化
# pylint>=2.0.0     # 代码检查
# mypy>=0.900       # 类型检查
